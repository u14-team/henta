import React, { type FC } from 'react';
// import { RawHTML } from 'react-dom';

const Raw: FC<{ children: string }> = ({ children }) => {
  // return <RawHTML>{{ __dangerousHTML: children }}</RawHTML>;
};

export default Raw;
